# Structure Steering

- Keep optional modules isolated under `src/modules/optional`
- Keep API surface versioned under `src/app/api/v1`
- Keep config and docs aligned
- Keep env schema in `src/lib/config/env.ts` in sync with `.env.example`
