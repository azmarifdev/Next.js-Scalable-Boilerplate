# Claude Notes

Project defaults:

- Next.js App Router + TypeScript
- Auth default: `better-auth` (internal API)
- Optional: `custom-auth` (external provider)

Always keep docs and env examples in sync when auth/config changes.
